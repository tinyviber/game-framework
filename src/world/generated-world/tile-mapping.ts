/**
 * Tile mapping — `terrainTileFor` / `plateauTileFor` / `assignTerrainTileIds`.
 * Currently in `index.ts:263-315`. Future split will move them here and
 * inject `TERRAIN_VARIANTS` via `TerrainTileResolver` from
 * `src/assets/kenney-map-pack/kenney-resolver.ts`.
 */
export {};
